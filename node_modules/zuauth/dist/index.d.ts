import { EdDSATicketFieldsToReveal } from '@pcd/zk-eddsa-event-ticket-pcd';

/**
 * Opens a Zupass popup to make a proof of a ZK EdDSA event ticket PCD.
 */
declare function openZKEdDSAEventTicketPopup(fieldsToReveal: EdDSATicketFieldsToReveal, watermark: string | bigint, externalNullifier?: string | bigint, validEventIds?: string[], validProductIds?: string[], popupRoute?: string, proofTitle?: string, proofDescription?: string): void;
declare function parseSerializedPCD(serializedPCD: string): string | undefined;
declare function isZupassPublicKey(publicKey: [string, string]): boolean;

declare function useZuAuth(): {
    authenticate: typeof openZKEdDSAEventTicketPopup;
    pcd?: string;
};

declare const ZUPASS_URL = "https://zupass.org";
declare const zupassPublicKey: [string, string];
declare const supportedEvents: string[];
declare const supportedProducs: string[];

/**
 * React hook that listens for PCDs and PendingPCDs from a Zupass popup window
 * using message passing and event listeners.
 */
declare function useZupassPopupMessages(): string[];
/**
 * A react hook that sets up necessary Zupass popup logic on a specific route.
 * A popup page must be hosted on the website that integrates with Zupass, as data can't
 * be passed between a website and a popup on a different origin like zupass.org.
 * This hook sends messages with a full client-side PCD or a server-side PendingPCD
 * that can be processed by the `useZupassPopupMessages` hook. PendingPCD requests
 * can further be processed by `usePendingPCD` and `usePCDMultiplexer`.
 */
declare function usePopup(): string;
/**
 * Open up a Zupass popup window using proofUrl from specific PCD integrations
 * and popupUrl, which is the route where the useZupassPopupSetup hook is being
 * served from.
 */
declare function openZupassPopup(popupUrl: string, proofUrl: string): void;

export { ZUPASS_URL, isZupassPublicKey, openZKEdDSAEventTicketPopup, openZupassPopup, parseSerializedPCD, supportedEvents, supportedProducs, usePopup, useZuAuth, useZupassPopupMessages, zupassPublicKey };
